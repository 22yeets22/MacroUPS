## CAD Readme

Quick note, the `MacroUPS3.step` file is the main version. The `USBC Stand.step` file is a file just to hold the usb-c modules in place. The assembly likely differs for everyone, so it may not even be used.
